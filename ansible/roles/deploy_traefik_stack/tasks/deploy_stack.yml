---
- name: deploy the traefik stack
  shell: docker stack deploy --compose-file {{ working_dir }}/dockerfiles/docker-compose-traefik.yml theoperator-traefik
  environment:
      WORKING_DIR: '{{ working_dir }}'
